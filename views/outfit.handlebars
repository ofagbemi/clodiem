<!doctype html>
<html>
  <head>
    <title>clodiem</title>
    <link rel="stylesheet" href="/css/bootstrap.css">
    <link rel="stylesheet" href="/css/bootstrap-theme.css">
    <link rel="stylesheet" href="/css/global.css">
    <meta content="width=device-width,initial-scale=1.0" name="viewport">
  </head>
  
  <body>
    <div class="content">
      <div class="posts">
        <!--Only one post in posts-->
        {{#each posts}}
		  {{> post}}
		
		  <div class="post post_comments">
			<div class="comments_section">
				<textarea class="comment_box" placeholder="ADD COMMENT..."></textarea>
				<div postid="{{id}}" class="button comment_button" style="float:right;margin-bottom:4px;">
				  <a postid="{{id}}" href="#">comment</a>
				</div>
			  <div class="clear"></div>
			  <!--Only one post in posts-->
			  <div class="comments" postid="{{id}}">
				{{#each comments}}
				  {{> comment}}
				{{/each}}
			  </div>
			</div>
		  </div>
		
		{{/each}}
	  </div>
    </div>
    
    <script src="/js/jquery-1.11.0.js"></script>
    <script src="/js/renderposts.js"></script>
    <script src="/js/rendermenu.js"></script>
    <script src="/js/rendersearchbar.js"></script>
    <script src="/js/rendertopbar.js"></script>
    <script src="/js/splitbackground.js"></script>
    <script src="/js/likebuttons.js"></script>
    <script src="/js/commentbuttons.js"></script>
    <script src="/js/comments.js"></script>
    <script>
      function setSizes() {
        rendertopbar(true);
        renderpost();
        rendercomments(
          {{#if logged_in_user}}
            {{logged_in_user.username}},
            {{logged_in_user.img}}
          {{/if}});
        
        // rendercomments('{{logged_in_username}}', '{{logged_in_user_img}}');
        buildmenu({{#if logged_in_user}}'{{logged_in_user.username}}'{{/if}});
      }
      $(window).resize(function() {
        setSizes();
      });
      $(document).ready(function() {
        setSizes();
        activatelikebuttons();
        activatecommentbuttons();
      });
      $(window).load(function() {
        setSizes();
      });
    </script>
  </body>

</html>