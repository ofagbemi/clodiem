<script>
  function setlinks() {
	$('a').click( function(e) {
	  var h = $(this).attr('href');
	  if(h == '#') h = null;
	  if(h && (h.indexOf('clodiem.') > -1 || h.indexOf('localhost') > -1 || h.indexOf('/') == 0)) {
	    e.preventDefault();
	    location.href = h;
	  }
	});
  }
  function setlinks_exp() {
	$('a').click( function(e) {
	  var h = $(this).attr('href');
	  if(h == '#') h = null;
	  // if(h) location.href = h;
	  if(h && (h.indexOf('clodiem.') > -1 || h.indexOf('localhost') > -1 || h.indexOf('/') == 0)) {
	    e.preventDefault();
	    $('#window').attr('src', h);
	  }
	});
  }
</script>